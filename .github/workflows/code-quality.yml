name: C# & TypeScript Quality, Formatting & Test Workflow

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  lint-analyze-test:
    runs-on: ubuntu-latest

    steps:
      # 0. JEDA / SLEEP 60 DETIK
      - name: Sleep for 60 seconds
        run: sleep 60

      # 1. CHECKOUT REPOSITORY
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}

      # 2. SETUP ENVIRONMENT (.NET & NODE.JS)
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          # Mengaktifkan cache npm di sub-folder frontend
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      # 3. BACKEND (.NET C#)
      - name: Restore .NET Dependencies
        run: dotnet restore
        working-directory: backend

      - name: Auto-Format C# Code
        run: dotnet format
        working-directory: backend

      - name: Build & Typecheck (.NET)
        run: dotnet build --configuration Release /p:TreatWarningsAsErrors=true
        working-directory: backend

      # 4. FRONTEND (Next.js & Playwright)
      - name: Install Frontend Dependencies
        run: npm ci
        working-directory: frontend

      - name: Run Prettier Check/Write (Frontend & Root)
        run: npx prettier --write .
        working-directory: frontend

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
        working-directory: frontend

      - name: Run Playwright Tests
        run: npx playwright test --pass-with-no-tests
        working-directory: frontend

      # 5. MOBILE (React Native) - Optional Linting
      - name: Install Mobile Dependencies
        run: npm ci
        working-directory: mobile

      - name: Run Prettier (Mobile)
        run: npx prettier --write .
        working-directory: mobile

      # 6. COMMIT AUTO-FORMATTED CHANGES (C# & Prettier)
      - name: Commit Formatted Changes
        id: auto_commit
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "style: auto-correct code formatting via dotnet format & prettier [skip ci]"
          commit_user_name: "github-actions[bot]"
          commit_user_email: "github-actions[bot]@users.noreply.github.com"

      # 7. UPLOAD PLAYWRIGHT REPORT
      - name: Upload Playwright Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: frontend/playwright-report/
          retention-days: 30
